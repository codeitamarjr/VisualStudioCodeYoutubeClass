public class LessonComparisnoOperators {

    public static void main(String[] args) {
        
        //we use comparison operators to compare primitive values
        int x = 1;
        int y = 2;
            System.out.println(x==y); //the equality operator == to compare if both are equal, it's called a boolean expression because will return true or false.
            System.out.println(x!=y); //the inequality operator != will compare if both are diferent and will return a boolean.
            //We also can use greather than >, greathen and equal to =>, less than <, less than or equal to <=
    }
    
}
